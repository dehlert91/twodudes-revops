export function StubPage({ title }) {
  return (
    <div className="flex items-center justify-center h-64">
      <p className="text-muted font-display text-2xl">{title} — coming soon</p>
    </div>
  )
}
